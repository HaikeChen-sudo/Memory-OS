/**
 * Card animation slots — MemoryCard animations.
 *
 * All exports are no-op stubs in MVP.
 * Replace with Framer Motion variants when animation phase begins.
 *
 * Rule: animation state NEVER pollutes business state (stores, types).
 * Animation is a pure rendering concern.
 */

export const cardEnter = {};
export const cardHover = {};
